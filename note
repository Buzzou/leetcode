1. 右指针 right 向右扩展窗口，不断加入新字符。
2. 如果发现重复字符，左指针 left 右移，缩小窗口(直接跳到该重复字符「在窗口内」位置的下一个位置：把之前的跳过去，因为准备囊括这个新的)，这样窗口内就又没有重复字符了。
3. 更新最大长度。

如何判断有重复字符？ 用 unordered_set 或 unordered_map（哈希表）存储窗口内的字符，快速查询是否重复。

注意：某字符 c 在窗口内需要 c 在哈希表中记录的位置是 >= left 的
