因为如果得不到1会无限循环（「循环」即会出现「重复」的数字），所以可以用 哈希集合（unordered_set） 来记录出现过的数字；
或者用 快慢指针法（Floyd 判环算法） 来优化。

思路
计算数位平方和：对于 n，计算它的所有数位的平方和，作为下一个 n。
判断终止条件：
✅ 如果 n 变成 1，返回 true。
❌ 如果 n 进入循环（即某个 n 之前出现过），返回 false。
如何检测循环？
使用 unordered_set<int> 存储出现过的 n，如果 n 重新出现，则说明进入循环。
